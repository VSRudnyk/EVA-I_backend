const createDatabase = require('./createDatabase');
const getDatabase = require('./getDatabase');
const auth = require('./auth');
const getCurrentBot = require('./getCurrentBot');
const getJs = require('./getJs');

module.exports = {
  createDatabase,
  getDatabase,
  auth,
  getCurrentBot,
  getJs,
};
